<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects Showroom - My Control Center</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="icon" type="image/svg+xml" href="./images/dcc_icon.svg">
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
</head>
<body>
    <header>
        <div class="header-brand">
            <img src="./images/dcc_icon.svg" alt="Control Center Icon" class="site-icon" style="width: 36px; height: 36px;">
            <h1>Projects Showroom</h1>
        </div>
        <nav class="site-nav">
            <a href="index.html">Home</a>
            <a href="snippets.html">Snippets</a>
            <a href="projects.html" class="active">Projects</a>
            <a href="tasks.html">Tasks</a>
            <a href="inspiration.html">Inspiration</a>
            <a href="ideas.html">Ideas</a>
            <a href="finance.html">Finance</a>
            <a href="planner.html">Web Planner</a>
        </nav>

        <div id="logout-container" class="header-user-widget" style="display: none;">
            <span id="user-display" class="user-email"></span>
            <button type="button" class="logout-action-btn" onclick="handleLogout()">Logout</button>
        </div>
    </header>

    <div id="auth-container" class="auth-container-wrapper" style="display: none;">
        <section class="card auth-card-box">
            <h2>🔐 Control Center Access</h2>
            <div class="auth-form-group">
                <input type="email" id="auth-email" class="auth-input" placeholder="Email address..." required autocomplete="email">
                <input type="password" id="auth-password" class="auth-input" placeholder="Password..." required autocomplete="current-password">
                <div class="auth-buttons-row">
                    <button type="button" class="auth-btn login-btn" onclick="handleLogin()">Login</button>
                </div>
                <div id="auth-error" class="auth-error-msg"></div>
            </div>
        </section>
    </div>

    <main class="page-block-layout">
        
        <!-- Add New Project Collapsible Dropdown Card -->
        <section class="card" style="margin-bottom: 2rem;">
            <div onclick="toggleAddProjectForm()" style="display: flex; justify-content: space-between; align-items: center; cursor: pointer; user-select: none;">
                <h2 style="margin: 0; color: #e3e8ef; font-size: 1.25rem;">➕ Add New Project</h2>
                <button type="button" id="form-toggle-btn" style="background: #272e38; border: 1px solid #3f4a5a; color: #61afef; cursor: pointer; font-size: 0.85rem; padding: 6px 14px; border-radius: 6px; font-weight: 500; pointer-events: none;">▼ Open</button>
            </div>
            
            <form id="add-project-form" onsubmit="handleAddProject(event)" style="display: none; flex-direction: column; gap: 14px; margin-top: 1.2rem; border-top: 1px solid #3f4a5a; padding-top: 1.2rem;">
                <input type="text" id="proj-title" class="auth-input" placeholder="Project Title (e.g., Elytra Models Add-on)" required autocomplete="off">
                <textarea id="proj-desc" class="auth-input" placeholder="Short description..." style="height: 90px; resize: vertical;" autocomplete="off"></textarea>
                
                <!-- Hidden input storing the final URL -->
                <input type="hidden" id="proj-link" value="">

                <!-- Link Attachment Trigger & Preview Container -->
                <div style="background: #151a21; border: 1px solid #3f4a5a; padding: 1rem; border-radius: 8px; display: flex; flex-direction: column; gap: 8px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 10px;">
                        <span style="font-size: 0.85rem; color: #e3e8ef; font-weight: 500;">External Link Attachment:</span>
                        <button type="button" onclick="openLinkModal()" style="background: #272e38; border: 1px solid #3f4a5a; color: #61afef; cursor: pointer; font-size: 0.85rem; padding: 6px 14px; border-radius: 6px; font-weight: 500; transition: background 0.2s;">🔗 Add / Edit Link</button>
                    </div>
                    <div id="link-preview-container" style="font-size: 0.85rem; color: #9aa5b1; display: none; align-items: center; justify-content: space-between; background: #1e242d; padding: 8px 12px; border-radius: 6px; border: 1px solid #3f4a5a;">
                        <span id="link-preview-text" style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 280px; color: #61afef;"></span>
                        <button type="button" onclick="removeAttachedLink()" style="background: none; border: none; color: #e06c75; cursor: pointer; font-size: 0.8rem; font-weight: 500;">Remove</button>
                    </div>
                </div>
                
                <!-- Thumbnail Image Container -->
                <div style="display: flex; flex-direction: column; gap: 6px; background: #151a21; border: 1px solid #3f4a5a; padding: 1rem; border-radius: 8px;">
                    <label style="font-size: 0.85rem; color: #e3e8ef; font-weight: 500;">Project Thumbnail Image:</label>
                    <div style="display: flex; align-items: center; gap: 12px; flex-wrap: wrap;">
                        <input type="file" id="proj-image-file" accept="image/*" style="display: none;" onchange="updateFileName(this, 'proj-file-name')">
                        <label for="proj-image-file" style="background: #272e38; border: 1px solid #3f4a5a; color: #61afef; cursor: pointer; font-size: 0.85rem; padding: 8px 16px; border-radius: 6px; font-weight: 500; display: inline-block; transition: background 0.2s;">📁 Choose Image</label>
                        <span id="proj-file-name" style="font-size: 0.85rem; color: #9aa5b1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">No file chosen</span>
                    </div>
                </div>

                <button type="submit" class="auth-btn login-btn" style="align-self: flex-start; padding: 0.65rem 1.6rem; margin-top: 0.5rem;">Create Project Page</button>
            </form>
        </section>

        <!-- Dynamic Projects Grid Container -->
        <div id="projects-list-container" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(310px, 1fr)); gap: 1.5rem;"></div>

    </main>

    <footer>
        <p>&copy; 2026 My Control Center</p>
    </footer>

    <script src="script.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', async () => {
            const { data: { session } } = await supabaseClient.auth.getSession();
            if (session) {
                fetchProjects();
            }
        });

        // Toggle Add Project Form Dropdown
        function toggleAddProjectForm() {
            const form = document.getElementById('add-project-form');
            const btn = document.getElementById('form-toggle-btn');
            if (form.style.display === 'none' || form.style.display === '') {
                form.style.display = 'flex';
                btn.textContent = '▲ Close';
            } else {
                form.style.display = 'none';
                btn.textContent = '▼ Open';
            }
        }

        async function fetchProjects() {
            const container = document.getElementById('projects-list-container');
            if (!container) return;
            
            container.innerHTML = '<p style="opacity: 0.7; text-align: center; grid-column: 1 / -1; padding: 2rem;">Loading projects...</p>';
            
            const { data, error } = await supabaseClient
                .from('projects')
                .select('*')
                .order('created_at', { ascending: false });

            if (error) {
                console.error('Error fetching projects:', error.message);
                container.innerHTML = '<p style="color: #e06c75; text-align: center; grid-column: 1 / -1; padding: 2rem;">Failed to load projects.</p>';
                return;
            }

            if (!data || data.length === 0) {
                container.innerHTML = '<p style="opacity: 0.7; text-align: center; grid-column: 1 / -1; padding: 3rem; background: #1e242d; border: 1px solid #3f4a5a; border-radius: 8px;">No projects added yet. Use the dropdown above to create one!</p>';
                return;
            }

            container.innerHTML = '';
            data.forEach(proj => {
                const card = document.createElement('section');
                card.className = 'card';
                card.style.cssText = 'display: flex; flex-direction: column; justify-content: space-between; margin: 0; height: 100%; transition: transform 0.2s ease, border-color 0.2s ease; box-sizing: border-box;';
                
                card.onmouseenter = () => { card.style.borderColor = '#61afef'; card.style.transform = 'translateY(-2px)'; };
                card.onmouseleave = () => { card.style.borderColor = '#3f4a5a'; card.style.transform = 'translateY(0)'; };

                const imageThumbnail = proj.image_url 
                    ? `<div style="width: 100%; height: 150px; background: #151a21; border-radius: 6px; border: 1px solid #3f4a5a; overflow: hidden; margin-bottom: 1rem;"><img src="${proj.image_url}" alt="Project Image" style="width: 100%; height: 100%; object-fit: cover;"></div>` 
                    : `<div style="width: 100%; height: 90px; background: #151a21; border-radius: 6px; border: 1px solid #3f4a5a; display: flex; align-items: center; justify-content: center; margin-bottom: 1rem; color: #9aa5b1; font-size: 1.5rem;">📁</div>`;

                const externalLinkBadge = proj.external_link 
                    ? `<a href="${proj.external_link}" target="_blank" style="color: #61afef; text-decoration: none; font-size: 0.85rem; font-weight: 500; display: inline-flex; align-items: center; gap: 4px;" title="External Link">🔗 Visit Link</a>` 
                    : '';

                card.innerHTML = `
                    <div>
                        ${imageThumbnail}
                        <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; margin-bottom: 0.5rem;">
                            <h2 style="margin: 0; color: #e3e8ef; font-size: 1.2rem; line-height: 1.4;">${escapeHtml(proj.title)}</h2>
                            ${externalLinkBadge}
                        </div>
                        <p style="margin-bottom: 1.5rem; opacity: 0.85; color: #9aa5b1; font-size: 0.92rem; line-height: 1.5;">${escapeHtml(proj.description || 'No description provided.')}</p>
                    </div>
                    <div style="display: flex; justify-content: space-between; align-items: center; border-top: 1px solid #3f4a5a; padding-top: 1rem; margin-top: auto;">
                        <a href="project.html?id=${proj.id}" style="color: #61afef; text-decoration: none; font-weight: 500; font-size: 0.9rem;">Open Hub &rarr;</a>
                        <button type="button" onclick="openDeleteModal('${proj.id}')" style="background: #272e38; border: 1px solid #3f4a5a; color: #e06c75; cursor: pointer; font-size: 0.82rem; padding: 6px 12px; border-radius: 6px; font-weight: 500; transition: background 0.2s;" title="Delete Project">🗑️ Delete</button>
                    </div>
                `;
                container.appendChild(card);
            });
        }

        // Link Modal Controls
        function openLinkModal() {
            const currentLink = document.getElementById('proj-link').value;
            document.getElementById('modal-link-url').value = currentLink;
            document.getElementById('link-modal').style.display = 'flex';
        }

        function closeLinkModal() {
            document.getElementById('link-modal').style.display = 'none';
        }

        function saveModalLink() {
            const urlInput = document.getElementById('modal-link-url').value.trim();
            const hiddenInput = document.getElementById('proj-link');
            const previewContainer = document.getElementById('link-preview-container');
            const previewText = document.getElementById('link-preview-text');

            if (urlInput) {
                hiddenInput.value = urlInput;
                previewText.textContent = urlInput;
                previewContainer.style.display = 'flex';
            } else {
                hiddenInput.value = '';
                previewContainer.style.display = 'none';
            }
            closeLinkModal();
        }

        function removeAttachedLink() {
            document.getElementById('proj-link').value = '';
            document.getElementById('link-preview-container').style.display = 'none';
            document.getElementById('modal-link-url').value = '';
        }

        // Helper to update displayed filename when file is picked
        function updateFileName(input, targetId) {
            const span = document.getElementById(targetId);
            if (!span) return;
            if (input.files && input.files.length > 0) {
                span.textContent = input.files[0].name;
                span.style.color = '#e3e8ef';
            } else {
                span.textContent = 'No file chosen';
                span.style.color = '#9aa5b1';
            }
        }

        // Helper to convert a file input to Base64 string
        function convertFileToBase64(fileInput) {
            return new Promise((resolve, reject) => {
                const file = fileInput.files[0];
                if (!file) {
                    resolve(null);
                    return;
                }
                const reader = new FileReader();
                reader.onload = () => resolve(reader.result);
                reader.onerror = error => reject(error);
                reader.readAsDataURL(file);
            });
        }

        async function handleAddProject(event) {
            event.preventDefault();
            const title = document.getElementById('proj-title').value.trim();
            const description = document.getElementById('proj-desc').value.trim();
            const external_link = document.getElementById('proj-link').value.trim();
            const fileInput = document.getElementById('proj-image-file');

            if (!title) {
                alert('Please provide a project title.');
                return;
            }

            try {
                const image_url = await convertFileToBase64(fileInput);

                const { error } = await supabaseClient
                    .from('projects')
                    .insert([{ title, description, external_link, image_url }]);

                if (error) {
                    console.error('Error adding project:', error.message);
                    alert('Failed to add project: ' + error.message);
                } else {
                    document.getElementById('add-project-form').reset();
                    removeAttachedLink();
                    const fileNameSpan = document.getElementById('proj-file-name');
                    if (fileNameSpan) {
                        fileNameSpan.textContent = 'No file chosen';
                        fileNameSpan.style.color = '#9aa5b1';
                    }
                    toggleAddProjectForm(); 
                    fetchProjects();
                }
            } catch (err) {
                console.error('Image processing error:', err);
                alert('Failed to process image file.');
            }
        }

        // Mobile-Friendly Delete Modal Controls
        function openDeleteModal(id) {
            document.getElementById('delete-proj-id').value = id;
            const modal = document.getElementById('delete-project-modal');
            if (modal) modal.style.display = 'flex';
        }

        function closeDeleteModal() {
            const modal = document.getElementById('delete-project-modal');
            if (modal) modal.style.display = 'none';
        }

        async function confirmDeleteProject() {
            const id = document.getElementById('delete-proj-id').value;
            const { error } = await supabaseClient
                .from('projects')
                .delete()
                .eq('id', id);

            if (error) {
                console.error('Error deleting project:', error.message);
                alert('Failed to delete project.');
            } else {
                closeDeleteModal();
                fetchProjects();
            }
        }

        function escapeHtml(str) {
            return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
        }
    </script>

    <!-- Custom Link Input Modal -->
    <div id="link-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.7); z-index: 9999; justify-content: center; align-items: center; padding: 1rem; box-sizing: border-box;">
        <div class="card" style="max-width: 420px; width: 100%; background: #1e242d; border: 1px solid #3f4a5a; padding: 2rem; box-shadow: 0 8px 24px rgba(0,0,0,0.5);">
            <h3 style="margin-top: 0; color: #e3e8ef; margin-bottom: 0.5rem;">🔗 Attach External Link</h3>
            <p style="color: #9aa5b1; font-size: 0.88rem; margin-bottom: 1.2rem;">Enter the destination URL for this project.</p>
            <div style="display: flex; flex-direction: column; gap: 12px;">
                <input type="url" id="modal-link-url" class="auth-input" placeholder="https://example.com" autocomplete="off">
                <div style="display: flex; gap: 10px; margin-top: 0.5rem;">
                    <button type="button" class="auth-btn" style="background: #272e38; border: 1px solid #3f4a5a; color: #e3e8ef; flex: 1;" onclick="closeLinkModal()">Cancel</button>
                    <button type="button" class="auth-btn login-btn" style="flex: 1; background: #61afef; color: #fff;" onclick="saveModalLink()">Attach Link</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Delete Confirmation Modal -->
    <div id="delete-project-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.7); z-index: 9999; justify-content: center; align-items: center; padding: 1rem; box-sizing: border-box;">
        <div class="card" style="max-width: 400px; width: 100%; text-align: center; background: #1e242d; border: 1px solid #3f4a5a; padding: 2rem; box-shadow: 0 8px 24px rgba(0,0,0,0.5);">
            <h3 style="margin-top: 0; color: #e3e8ef;">Delete Project</h3>
            <p style="color: #9aa5b1; font-size: 0.9rem; margin-bottom: 1.5rem;">Are you sure you want to delete this project card?</p>
            <input type="hidden" id="delete-proj-id">
            <div style="display: flex; gap: 10px; justify-content: center;">
                <button type="button" class="auth-btn" style="background: #272e38; border: 1px solid #3f4a5a; color: #e3e8ef; flex: 1;" onclick="closeDeleteModal()">Cancel</button>
                <button type="button" class="auth-btn login-btn" style="flex: 1; background: #e06c75;" onclick="confirmDeleteProject()">Delete</button>
            </div>
        </div>
    </div>

    <!-- Custom Logout Confirmation Modal -->
    <div id="logout-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.7); z-index: 9999; justify-content: center; align-items: center; padding: 1rem; box-sizing: border-box;">
        <div class="card" style="max-width: 400px; width: 100%; text-align: center; background: #1e242d; border: 1px solid #3f4a5a; padding: 2rem; box-shadow: 0 8px 24px rgba(0,0,0,0.5);">
            <h3 style="margin-top: 0; color: #e3e8ef;">Confirm Logout</h3>
            <p style="color: #9aa5b1; font-size: 0.9rem; margin-bottom: 1.5rem;">Are you sure you want to log out of your Control Center?</p>
            <div style="display: flex; gap: 10px; justify-content: center;">
                <button type="button" class="auth-btn" style="background: #272e38; border: 1px solid #3f4a5a; color: #e3e8ef; flex: 1;" onclick="closeLogoutModal()">Cancel</button>
                <button type="button" class="auth-btn login-btn" style="flex: 1; background: #e06c75;" onclick="confirmLogout()">Log Out</button>
            </div>
        </div>
    </div>
</body>
</html>